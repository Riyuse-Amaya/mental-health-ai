from setuptools import setup, find_packages

setup(
    name='mlask',
    version='0.1',
    packages=find_packages(),
    install_requires=[],
    author='あなたの名前',
    description='MLAsk 感情分析ツール（カスタム版）',
)
